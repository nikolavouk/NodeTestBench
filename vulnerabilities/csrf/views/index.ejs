<% include ../../../views/partials/header %>


        <h1 class="page-header">Cross Site Request Forgery</h1>

        <div class="row">
          <div class="col-xs-12 col-sm-6" style="padding-bottom: 30px;">
                <% if(csrfToken == 'none') { %>
                  <h4 class="sub-header">Vulnerable Form</h4>
                  <form method="POST" id="vulnerable-form" action="/csrf/csrf-form-unprotected">
                      <div class="form-group">
                        <label>Name</label>
                        <input name="name" class="form-control" id="Name" placeholder="" value="Joe Danger">
                      </div>
                <% } else { %>
                    <h4 class="sub-header">Safe Form</h4>
                    <form method="POST" id="safer-form" action="/csrf/csrf-form-protected">
                        <div class="form-group">
                          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                          <label>Name</label>
                          <input name="name" class="form-control" id="Name" placeholder="" value="Joseph Safe">
                        </div>
                <% } %>
                    <span> Csrf Token: </span> <%= csrfToken %>
                    <div><br></div>
                    <button type="submit" class="btn btn-default">Submit</button>
                  </form>

          </div>
        </div>

<% include ../../../views/partials/footer %>
